# 把审计里的四件事做完

**状态：** 完成，2026-08-25。
来源：`docs/plans/2026-08-25-audit-fakes-and-performance.md`。

## 四件事

| # | 事情 | 为什么 |
| --- | --- | --- |
| 1 | `outgoing(from:)` 改成字典 | 现在每次调用都过滤全部 edge，O(E)，每开一篇笔记调一次 |
| 2 | 六个死设置 | 有开关没读者。会撒谎的开关比没有功能更糟 |
| 3 | 搜索去抖 + 移出主线程 | 实测每次按键 783 ms 卡死主线程 |
| 4 | 真正的阅读渲染器 | 现在 `.reading` 只做了 `isEditable = false` |

## 验收标准

- [x] `outgoing` 是 O(1) 查表，有测试证明它和旧实现结果一致
- [x] 六个设置每一个都有：真实行为 + 单元测试（关掉时行为确实不同）
- [x] 搜索不在主线程；连打字不触发多余查询；有取消
- [x] 阅读模式和实时预览**看得出区别**，且这个区别有测试覆盖
- [x] `scripts/test-all.sh` 全绿（411 个 core 测试）
- [x] 全部做完后重新跑一次性能采样，能改的继续改

## 性能：实测数字

搜索，8,852 篇笔记，release 构建：

| 查询 | 原来（串行、主线程） | 现在（分批并行、非主线程） |
| --- | --- | --- |
| 匹配不到任何东西 | 778 ms | **115 ms** |
| 匹配很多 | 257 ms | **21 ms** |

**真凶不是并行度，是排序。** 第一版并行改完，"匹配不到"从 774 ms 降到 102 ms，
但"匹配很多"反而从 110 ms 涨到 178 ms —— 因为串行版能在凑够 200 条时提前退出。
改成分批之后两边**都更慢了**（525 / 416 ms），这才发现每次搜索都在对 8,852 个
`URL` 按 `path(percentEncoded:)` 排序，而那个方法每次比较都构造一个 String。
排序移到索引构建时做一次（`IndexSnapshot.orderedNotes`），两边同时变快。

连续输入搜索词时对主线程采样：**`SearchEngine` 的符号一个都不出现**，
`__CFRUNLOOP_IS_CALLING_OUT_TO_A_SOURCE0` 只有 2 个采样（0.0%）。

## 一个记下来的教训

三次测量，三次结论不同，只有最后一次是对的。中间那次"分批更慢"如果当成
"分批这个思路不行"就会走错路 —— 它真正说明的是**还有个更大的开销没找到**。
矛盾本身就是发现。

## 阅读模式丢内容的两个 bug（08-26 报告并修复）

阅读模式上线后，一篇用**全角冒号**写的 frontmatter（`编号：N19`）在阅读模式下
整块消失了。**同一个错误我在实时预览里已经犯过一次并修过**，写阅读渲染器时又
无条件 `cuts.append(整块)` 犯了第二次。

为了不靠人眼一个个找，先写了一个把各类语法都过一遍、打印"进什么/出什么"的
临时检查。它一次找出两个：

| | 原来 | 现在 |
| --- | --- | --- |
| frontmatter | 整块删掉，属性全丢 | 只删 `---` 围栏，内容保留并按次要样式排 |
| `%%注释%%` | **删掉整行**，`正文 %%注释%% 继续` 渲染成空 | 只删注释本身 |

顺带处理的、原来没渲染的：

- `> [!note] 标题` 的 `[!note]` 标记去掉，标题保留
- `$$` 公式围栏去掉，公式保留
- 表格的 `| --- | --- |` 对齐行去掉（它不含任何内容，只是给解析器看的）

### 防再犯

`losesNoContent` 是一条属性测试，不是一个例子：把源文里每一段字母/数字/中日韩
序列取出来，要求渲染后**每一个都还在**。语法是标点，所以这恰好就是"不许动"的
那个集合。两个有意的例外写在测试里而不是绕开规则 —— `%%注释%%` 本来就该消失，
`[[目标|别名]]` 本来就该显示别名。

新增一类块要是开始吃字，这条测试会立刻炸。

## 阅读模式补齐渲染（08-26）

图片、Mermaid 图、数学公式现在都画出来了，表格也排成对齐的列。

**关键点：阅读模式可以用 `NSTextAttachment`，编辑器不行。** 编辑器的 storage
就是磁盘上的文件，而 TextKit 只给 U+FFFC 生成附件字形 —— 往文件里插一个用户没
打的字符是不行的，这就是编辑器为什么要"预留高度 + 手绘"。阅读视图的 storage
是我自己造的、不属于任何人，附件字符随便用，实现因此简单得多。

每一种都有降级：图片加载不出来就留文件名，公式解析不了就留 LaTeX 源码（用警告
色，和编辑器一致）。所以"渲染"永远不会变成"丢失"。

三个只有 Mermaid 是异步的（走 WKWebView），接了 `onRendered` 回调触发重排；
图片和公式的缓存都是同步返回。

### 中途踩的两个坑

1. **`GeometryReader` 把 `NSViewRepresentable` 压成了 0 高**，阅读模式整页空白。
   宽度改成在 `updateNSView` 里取（那里本来就有真实尺寸，而且图片缩放也必须在
   那里算）。
2. **误在用户的真实库里建了一个空笔记** —— 切库的 osascript 静默失败了，我没验证
   就用快速切换器打开笔记，找不到同名就创建了新的。已删除（0 字节，git 未跟踪）。
   教训：驱动 UI 的每一步都要验证，不能一把梭。

### 表格对齐

CJK 字符在等宽字体里占两列。按一列补齐会让它后面每一列都错位 —— 而排版这张表
的全部意义就是让列对齐。`alignedTable` 按显示宽度补，有测试钉住
`列一  b` / `a     乙` 这个具体输出。
